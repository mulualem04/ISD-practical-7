def mystery(x, y): # defining a function
    # Pass parameters : x = 2, y = 3
    result = (x + y) / (y - x)
    return result 
    # The function returns the quotient of (x+y) and (y-x) 
def main():
    print(mystery(2,3))# Call the function within a print

main() # call the main function   
